language: java
jdk: oraclejdk11

cache:
  directories:
  - $HOME/.m2

services:
  - docker  

script: ./build_docker.sh

deploy:
  - provider: script
    script: ./build_docker.sh 
    on:
      branch: master  

    

